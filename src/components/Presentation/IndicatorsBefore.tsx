import React from 'react';
import styles from './../../styles.module.css';

const IndicatorsBefore = () => {
  return <div className={`${styles['link']} ${styles['to-parent']}`} />;
};

export default IndicatorsBefore;
